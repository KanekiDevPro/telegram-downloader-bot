aiogram>=3.7,<4.0
aiohttp>=3.9
asyncpg>=0.29
pydantic>=2.7
pydantic-settings>=2.3
redis>=5.0
# yt-dlp plugin: fetches YouTube PO tokens from a provider (see YTDLP_POT_PROVIDER_URL).
yt-dlp>=2024.12.13
bgutil-ytdlp-pot-provider>=2.0.0
# SOCKS support for yt-dlp: the WARP tunnel serves HTTP *and* SOCKS5 on one port
# (see the warp service in docker-compose.yml), so `YTDLP_PROXY=socks5://warp:1080`
# is a legitimate choice — and yt-dlp needs this for it. Missing it fails every
# download with an unhelpful proxy error, so it is a dependency rather than a note.
PySocks>=1.7.1